Act as the incident-response engineer picking up a stalled intrusion review. The collector truncated the authoritative event timeline at `/app/data/event_timeline.json` and left the correlation engine at `/app/workflow/correlate_incidents.go` grouping activity under draft rules the response board later reversed. Nothing the engine reports can be trusted until that timeline is rebuilt. A pre-truncation snapshot and a journal of the changes the collector posted after it survive beside the truncated file under `/app/data`, along with the sensor registry that records how far each sensor's clock sits from the reference. How the two combine, what a retraction does, what a later restore brings back, how the recorded stamps reach the reference clock and the order of the result are governance decisions, and the timeline must be restored at its expected path first.

Then restore the engine, a Go program built with the toolchain installed here and staying a single `package main`. Preserve its `--input` and `--output-dir` options and their defaults; the triage policy and the sensor registry are always read from their fixed absolute paths under `/app/data`, and `--input` selects the timeline only. `/app/docs/triage_contract.json` is the output contract: paths, schemas, required-field lists, container shapes and sort orders. It says nothing about how any value is derived. Reconstruct that from `/app/incident/response_governance_log.md`, mostly routine shift notes, where the later dated decision governs. A run writes exactly `/app/output/summary.json`, `/app/output/incident_chains.json` and `/app/output/triage_queue.jsonl` inside the contract's 90-second budget. The recovery sources and the registers a run reads are read and not rewritten: the pre-truncation snapshot, the collector journal, the sensor registry, the triage policy and the shift log all come back byte-identical, and the timeline is the one thing under `/app/data` that a run replaces. Derive every value from the operational inputs: correct against an alternate timeline, identical across reruns, and leave the frozen snapshot `/app/workflow/.correlate_incidents.original.go` exactly as it is.
